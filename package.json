{
  "name": "english-speaking-app",
  "version": "1.0.0",
  "scripts": {
      "dev": "docker compose up --build",
    "dev:local": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uv run uvicorn app.main:app --reload\"",
    "commit": "cz"
  },
  "config": {
    "commitizen": {
      "path": "cz-customizable"
    }
  },
  "devDependencies": {
    "commitizen": "^4.3.1",
    "concurrently": "^9.2.1",
    "cz-conventional-changelog": "^3.3.0",
    "cz-customizable": "^7.5.4"
  }
}
